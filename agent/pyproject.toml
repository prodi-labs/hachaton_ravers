[project]
name = "block-canvas-agent"
version = "0.1.0"
description = "Block Canvas Agent — adds blocks to the frontend via ADK"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "python-dotenv",
  "pydantic",
  "google-adk",
  "google-genai",
  "ag-ui-adk",
]
